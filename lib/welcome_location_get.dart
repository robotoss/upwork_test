import 'package:flutter/material.dart';

class WelcomeLocationGet extends StatelessWidget {
  const WelcomeLocationGet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Welcome Location Get'),
      ),
      body: Center(
        child: Text('Welcome Location Get'),
      ),
    );
  }
}

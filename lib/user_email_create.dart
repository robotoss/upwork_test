import 'package:flutter/material.dart';

class UserEmailCreate extends StatelessWidget {
  const UserEmailCreate({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('User Email Create'),
      ),
      body: Center(
        child: Text('User Email Create'),
      ),
    );
  }
}
